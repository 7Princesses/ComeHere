<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/nav_home"
        app:showAsAction="always"
        android:icon="@drawable/icon_nav_home"
        android:enabled="true"
        android:checked="true" />

    <item android:id="@+id/nav_postwrite"
        app:showAsAction="always"
        android:icon="@drawable/icon_nav_postwrite"
        android:enabled="true"/>

    <item android:id="@+id/nav_chat"
        app:showAsAction="always"
        android:icon="@drawable/icon_nav_chat"
        android:enabled="true" />

    <item android:id="@+id/nav_mypage"
        app:showAsAction="always"
        android:icon="@drawable/icon_nav_mypage"
        android:enabled="true" />
     </menu>